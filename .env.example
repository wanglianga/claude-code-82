# 宿主机发布端口（容器内固定 4000，切勿直接写死宿主端口，避免并行任务冲突）
CC_PUBLISH_PORT=3082
# compose 项目名（由评估环境固定；本地可自定义）
COMPOSE_PROJECT_NAME=community-pool
# 应用容器内监听端口（一般无需修改）
PORT=4000
# JSON 数据文件路径（容器内，挂载到命名卷 pool-data）
DATA_FILE=/data/pool-db.json
